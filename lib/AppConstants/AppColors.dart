import 'package:flutter/material.dart';

class AppColors {
  static final Color transparent = Colors.transparent;

  static final Color whiteColor = Colors.white;
  static final Color blackColor = Colors.black;
  static final Color darkGreyColor = Color(0xFF374458);
  static final Color darkSlateGreyColor = Color(0xFF99a1ab);

  static final Color errorColor = Colors.red;
  static final Color warningColor = Colors.yellow[300];
}
